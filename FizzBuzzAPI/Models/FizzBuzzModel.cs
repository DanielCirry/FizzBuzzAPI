namespace FizzBuzzAPI.Models
{
    // I added Model just because I don't remember if "FizzBuzz" already exists somewhere else
    public class FizzBuzzModel
    {
        public object? Value { get; set; }
    }
}
